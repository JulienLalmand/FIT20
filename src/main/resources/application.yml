spring:

  mvc:
    view:
      prefix: /WEB-INF/jsp/
      suffix: .jsp


  datasource:
    url: jdbc:mysql://localhost/fit20
    username: fit20user
    password: fit20userpw

  jpa:
    properties:
      hibernate:
        current_session_context_class: org.springframework.orm.hibernate4.SpringSessionContext